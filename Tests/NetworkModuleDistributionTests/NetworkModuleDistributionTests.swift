import XCTest
@testable import NetworkModuleDistribution

final class NetworkModuleDistributionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetworkModuleDistribution().text, "Hello, World!")
    }
}
